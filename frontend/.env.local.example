# Backend API URL (used by the frontend in dev mode)
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL for real-time alerts (used by the frontend in dev mode)
NEXT_PUBLIC_WS_URL=ws://localhost:8000/api/ws/alerts
